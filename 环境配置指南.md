# AnySplat 环境配置指南

## 系统要求

- **操作系统**: Linux (推荐 Ubuntu 18.04+)
- **Python版本**: 3.10 或更高
- **CUDA版本**: 12.1 (或兼容版本)
- **GPU**: 支持CUDA的NVIDIA显卡

---

## 配置步骤

### 第 1 步：检查系统环境

在开始之前，请运行以下命令检查你的系统：

```bash
# 检查Python版本
python --version
python3 --version

# 检查CUDA版本（如果已安装）
nvcc --version

# 检查GPU信息
nvidia-smi
```

**请将上述命令的输出结果反馈给我，我会根据你的系统情况给出具体建议。**

---

### 第 2 步：安装 Conda（如果尚未安装）

如果你还没有安装 Conda，可以选择以下方式之一：

#### 选项 A：安装 Miniconda（推荐，更轻量）
```bash
# 下载 Miniconda 安装脚本
wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh

# 运行安装脚本
bash Miniconda3-latest-Linux-x86_64.sh

# 按照提示完成安装，建议选择 yes 初始化 conda
```

#### 选项 B：安装 Anaconda（功能更全）
```bash
# 下载 Anaconda 安装脚本
wget https://repo.anaconda.com/archive/Anaconda3-2024.02-1-Linux-x86_64.sh

# 运行安装脚本
bash Anaconda3-2024.02-1-Linux-x86_64.sh
```

安装完成后，重启终端或运行：
```bash
source ~/.bashrc
```

检查 conda 是否安装成功：
```bash
conda --version
```

---

### 第 3 步：创建 AnySplat 虚拟环境

```bash
# 进入项目目录
cd /path/to/anysplat  # 替换为你的项目路径

# 创建 Python 3.10 虚拟环境
conda create -y -n anysplat python=3.10

# 激活环境
conda activate anysplat
```

激活成功后，你的命令行提示符应该会显示 `(anysplat)`

---

### 第 4 步：安装 PyTorch（CUDA 12.1 版本）

```bash
# 确保在 anysplat 环境中
conda activate anysplat

# 安装 PyTorch 2.2.0 with CUDA 12.1
pip install torch==2.2.0 torchvision==0.17.0 torchaudio==2.2.0 --index-url https://download.pytorch.org/whl/cu121
```

**安装后验证 PyTorch：**
```bash
python -c "import torch; print(f'PyTorch版本: {torch.__version__}'); print(f'CUDA可用: {torch.cuda.is_available()}'); print(f'CUDA版本: {torch.version.cuda}')"
```

**请将输出结果反馈给我，确认 CUDA 是否可用。**

---

### 第 5 步：安装项目依赖

```bash
# 确保在项目目录中
cd /path/to/anysplat

# 安装所有依赖包
pip install -r requirements.txt
```

**注意事项：**
- `requirements.txt` 中包含一些特殊的依赖（如 pytorch3d 和 gsplat），可能需要较长时间编译
- 如果遇到安装错误，请将错误信息完整地反馈给我

---

### 第 6 步：验证安装

运行以下 Python 脚本验证关键依赖是否安装成功：

```python
import sys
print("=" * 50)
print("AnySplat 环境检查")
print("=" * 50)

# 检查 PyTorch
try:
    import torch
    print(f"✓ PyTorch: {torch.__version__}")
    print(f"  - CUDA 可用: {torch.cuda.is_available()}")
    if torch.cuda.is_available():
        print(f"  - CUDA 版本: {torch.version.cuda}")
        print(f"  - GPU 数量: {torch.cuda.device_count()}")
        print(f"  - GPU 名称: {torch.cuda.get_device_name(0)}")
except Exception as e:
    print(f"✗ PyTorch 导入失败: {e}")

# 检查关键依赖
packages = [
    "numpy", "tqdm", "lightning", "hydra", "einops",
    "PIL", "cv2", "timm", "lpips", "open3d",
    "gradio", "huggingface_hub", "safetensors"
]

for pkg in packages:
    try:
        if pkg == "PIL":
            __import__("PIL")
        elif pkg == "cv2":
            __import__("cv2")
        elif pkg == "hydra":
            __import__("hydra")
        else:
            __import__(pkg)
        print(f"✓ {pkg}")
    except ImportError:
        print(f"✗ {pkg} 未安装")

# 检查 pytorch3d
try:
    import pytorch3d
    print(f"✓ pytorch3d: {pytorch3d.__version__}")
except Exception as e:
    print(f"✗ pytorch3d 导入失败: {e}")

# 检查 gsplat
try:
    import gsplat
    print(f"✓ gsplat")
except Exception as e:
    print(f"✗ gsplat 导入失败: {e}")

# 检查 torch_scatter
try:
    import torch_scatter
    print(f"✓ torch_scatter")
except Exception as e:
    print(f"✗ torch_scatter 导入失败: {e}")

# 检查 xformers
try:
    import xformers
    print(f"✓ xformers: {xformers.__version__}")
except Exception as e:
    print(f"✗ xformers 导入失败: {e}")

print("=" * 50)
print("检查完成！")
print("=" * 50)
```

将上述代码保存为 `check_env.py`，然后运行：
```bash
python check_env.py
```

**请将完整的输出结果反馈给我。**

---

### 第 7 步：下载模型并测试

如果环境安装成功，可以尝试运行 Gradio Demo：

```bash
# 在项目目录中运行
python demo_gradio.py
```

这会自动从 Hugging Face 下载预训练模型并启动交互界面。

**如果成功，你会看到一个本地网址（如 http://127.0.0.1:7860），可以在浏览器中打开。**

---

## 常见问题排查

### 问题 1：CUDA 不可用
- 检查 NVIDIA 驱动是否正确安装：`nvidia-smi`
- 确认 CUDA toolkit 版本与 PyTorch 版本匹配
- 如果没有 GPU，可以尝试 CPU 版本（性能会很慢）

### 问题 2：pytorch3d 编译失败
```bash
# 可能需要先安装依赖
conda install -c conda-forge ffmpeg
pip install fvcore iopath
```

### 问题 3：gsplat 安装失败
- 确保 PyTorch 版本为 2.2.0
- 确保 CUDA 版本为 12.1
- 如果 wheel 文件下载失败，可以手动下载后安装

### 问题 4：内存不足
- 确保系统至少有 16GB RAM
- 如果是训练模型，建议 32GB+ RAM 和高端 GPU

---

## 开始配置

现在请从 **第 1 步** 开始，运行命令并把结果反馈给我。我会根据你的实际情况提供针对性的指导！
